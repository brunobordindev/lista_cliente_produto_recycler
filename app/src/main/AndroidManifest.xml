<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.listacadastroclientesprodutos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListaCadastroClientesProdutos">
        <activity
            android:name=".activity.AdicionarProdutosActivity"
            android:exported="false" />
        <activity
            android:name=".activity.AdicionarClienteActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ProdutoActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ClienteActivity"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>